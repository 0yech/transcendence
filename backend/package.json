{
  "name": "transcendence-backend",
  "version": "0.0.1",
  "private": true,
  "description": "Backend (NestJS) — lint scaffold; project to be bootstrapped with `nest new`.",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0"
  }
}
